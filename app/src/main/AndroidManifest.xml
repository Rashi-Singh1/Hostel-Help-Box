<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hostelhelpbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".AdminHome2"
            android:label="@string/title_activity_admin_home2"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".home2"
            android:label="@string/title_activity_home2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".SecyHome" />
        <activity android:name=".AdminHome" /> <!-- tools:replace="android:appComponentFactory"> -->
        <activity android:name=".Login" /> <!-- <activity android:name=".FragmentProfile" /> -->
        <activity android:name=".home" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>